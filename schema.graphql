type Transaction @entity {
  id: ID!                # Unique identifier (transaction hash)
  from: Bytes!           # Address that initiated the transaction
  to: Bytes!             # Contract address or recipient
  origin: Bytes          # Original address that started the transaction
  blockNumber: BigInt!   # Block number of the transaction
  timestamp: BigInt!     # Timestamp of the block containing the transaction
}